1. Skapa en klass med flera attribut

    Skapa en klass som heter Car med två attribut: brand (String) och year (int). Sätt brand till "Toyota" och year till 2020.
    Skapa ett objekt av klassen och skriv ut båda attributens värden.

2. Modifiera attribut i en klass

    Skapa en klass Person med ett attribut age som har standardvärdet 30.
    Skapa ett objekt av klassen och ändra värdet på age till 35. Skriv ut det nya värdet.

3. Använda final för att skydda attribut

    Skapa en klass Book med ett attribut title som är deklarerat som final och sätt det till "Java Programming".
    Försök att ändra värdet på title i huvudmetoden och observera vad som händer när du kompilerar.

4. Skapa flera objekt och ändra attribut

    Skapa en klass Animal med ett attribut species som har standardvärdet "Dog".
    Skapa två objekt av klassen. Ändra attributet species i ett av objekten till "Cat", men lämna det andra objektet oförändrat. Skriv ut attributet för båda objekten.

5. Skapa en klass med flera attribut och skriv ut deras värden

    Skapa en klass Student med attributen firstName, lastName, och grade (int). Sätt värdena till "Alice", "Johnson", och 90.
    Skapa ett objekt och skriv ut alla tre attributens värden i en sammanhängande mening.

